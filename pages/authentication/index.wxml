<view class="personal">
    <van-field label="姓名" model:value="{{ real_name }}" placeholder="请输入真实姓名" />
    <van-field label="身份证号" model:value="{{ id_num }}" placeholder="请输入身份证号" type="number" />
    <view class="order-sign" bindtap="handlesign">认证</view>
</view>